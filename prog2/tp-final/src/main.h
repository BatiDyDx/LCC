#ifndef MAIN_H
    #define MAIN_H
    #include "generador_descripcion.h"
    /*
    Ejecuta el programa principal, genera el archivo
    con la descripcion de la sopa de letras, retorna 1
    si hubo algun error, sino 0
    */
    int main(int argc, char const *argv[]);
#endif